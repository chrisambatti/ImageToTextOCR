<Window x:Class="ImageToTextOCR.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Invoice OCR - Executive View" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Information -->
        <StackPanel Grid.Row="0">
            <Button Content="Upload Invoice (PDF/Image)" Width="180" Height="35" Click="Upload_Click" Margin="0,0,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Company Name:" FontWeight="Bold"/>
                    <TextBlock Name="CompanyNameBox" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="Invoice No:" FontWeight="Bold"/>
                    <TextBlock Name="InvoiceNoBox" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="Date:" FontWeight="Bold"/>
                    <TextBlock Name="DateBox" FontSize="14" Margin="0,0,0,10"/>
                </StackPanel>

                <!-- Middle Column -->
                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="TRN:" FontWeight="Bold"/>
                    <TextBlock Name="TRNBox" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="Sales Person:" FontWeight="Bold"/>
                    <TextBlock Name="SalesPersonBox" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="Payment Terms:" FontWeight="Bold"/>
                    <TextBlock Name="PaymentTermsBox" FontSize="14" Margin="0,0,0,10"/>
                </StackPanel>

                <!-- Right Column -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Ship Date:" FontWeight="Bold"/>
                    <TextBlock Name="ShipDateBox" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="D.O. Number:" FontWeight="Bold"/>
                    <TextBlock Name="DONumberBox" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="S.O. Number:" FontWeight="Bold"/>
                    <TextBlock Name="SONumberBox" FontSize="14" Margin="0,0,0,10"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- Invoice Items Table -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Invoice Line Items:" FontWeight="Bold" FontSize="16" Margin="5" Background="LightBlue" Padding="5"/>

                <ScrollViewer Grid.Row="1">
                    <DataGrid Name="InvoiceItemsGrid" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="All"
                              AlternatingRowBackground="LightGray"
                              Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sr No." Binding="{Binding SrNo}" Width="60"/>
                            <DataGridTextColumn Header="Item Code" Binding="{Binding ItemCode}" Width="120"/>
                            <DataGridTextColumn Header="Item Description" Binding="{Binding ItemDescription}" Width="250"/>
                            <DataGridTextColumn Header="UOM" Binding="{Binding UOM}" Width="60"/>
                            <DataGridTextColumn Header="QTY" Binding="{Binding Quantity}" Width="80"/>
                            <DataGridTextColumn Header="Unit Rate" Binding="{Binding UnitRate}" Width="100"/>
                            <DataGridTextColumn Header="Total (Excl. VAT)" Binding="{Binding TotalExclVAT}" Width="120"/>
                            <DataGridTextColumn Header="VAT %" Binding="{Binding VATPercent}" Width="70"/>
                            <DataGridTextColumn Header="VAT Amount" Binding="{Binding VATAmount}" Width="100"/>
                            <DataGridTextColumn Header="Total (Incl. VAT)" Binding="{Binding TotalInclVAT}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>